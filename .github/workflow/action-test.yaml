name: hello_workflow

on:  
  push:
  branches:
    - main

jobs:
  print-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello world using echo
        run: echo cat hello.txt
